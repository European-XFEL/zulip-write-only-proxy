
name: zwop-dev

services:
  server:
    environment:
      - "ZWOP_TOKEN_WRITER__ZWOP_URL=http://localhost:8000/${COMPOSE_PROJECT_NAME}"
    develop:
      watch:
        - action: sync
          path: ./packages/zwop-service/src/zwop
          target: /app/packages/zwop-service/src/zwop
          ignore:
            - __pycache__/
            - "*.pyc"
            - ".pytest_cache/"
        - action: sync+restart
          path: ./pyproject.toml
          target: /app/pyproject.toml
        - action: sync+restart
          path: ./packages/zwop-service/pyproject.toml
          target: /app/packages/zwop-service/pyproject.toml
        - action: sync+restart
          path: ./packages/zwop-service/src/zwop/main.py
          target: /app/packages/zwop-service/src/zwop/main.py
    restart: never

  traefik:
    image: "traefik:latest"
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:8000"
      - "--log.level=DEBUG"
    ports:
      - 8000:8000
      - 8080:8080
    volumes:
      - ${DOCKER_SOCK:-/var/run/docker.sock}:/var/run/docker.sock:ro

  whoami:
    image: traefik/whoami
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whoami.rule=PathRegexp(`^/whoami(/|$)`)"
